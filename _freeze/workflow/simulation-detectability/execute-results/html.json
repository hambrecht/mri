{
  "hash": "67a21252c36cde7fca9cfc2d6b9d2a9d",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Simulating surveys with spatially varying detectability\"\nauthor: \"Leonard Hambrecht, IRSS, University of British Columbia\"\ndate: \"September 2026\"\nbibliography: references.bib\n---\n\n## Introduction\n\nIn the RMNP survey, moose were harder to detect under dense canopy: the [detection function](detection-function.qmd) with canopy cover (`pabove2`) was clearly better supported than one without it. Detectability therefore varies across the landscape, and so does the proportion of animals a survey misses. Whether this matters depends on the design. A design across the whole park samples all canopy conditions in proportion to their area, but subplots may by chance fall in denser or more open forest than the park as a whole.\n\nSimulations normally draw detectability from a fixed detection function, or from individual covariates such as group size that are unrelated to location. The [fork of `dsims`](https://github.com/hambrecht/dsims) adds *spatial* covariates: `make.detectability()` takes a raster through the new `cov.surface` argument, and at every repetition reads the raster at each simulated animal's location. The animal's scale parameter is then\n\n$$\\log(\\sigma_i) = \\log(\\sigma_0) + \\beta z_i,$$\n\nwhere $z_i$ is the raster value at the animal, $\\beta$ the coefficient in `cov.param`, and $\\sigma_0$ the scale parameter. This is the same log-linear form that `Distance` uses for covariates, so the fitted coefficients can be used directly. This article is the first documentation of the feature.\n\n## Prerequisites\n\n`cov.surface` exists only in the fork of `dsims`:\n\n```r\nremotes::install_github(c(\"hambrecht/dssd\", \"hambrecht/dsims\"))\n```\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(here)\nlibrary(dsims)\nlibrary(sf)\nlibrary(terra)\nlibrary(ggplot2)\nstopifnot(\"cov.surface\" %in% names(formals(make.detectability)))\n```\n:::\n\n\nWe load the region, population and detection function parameters from [*From density surface model to simulation*](simulation-density.qmd), and the designs from [*Survey designs with subplots*](simulation-design.qmd).\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlist2env(readRDS(here(\"workflow\", \"outputs\", \"simulation_setup.rds\")), envir = environment())\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n<environment: R_GlobalEnv>\n```\n\n\n:::\n\n```{.r .cell-code}\nlist2env(readRDS(here(\"workflow\", \"outputs\", \"simulation_designs.rds\")), envir = environment())\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n<environment: R_GlobalEnv>\n```\n\n\n:::\n:::\n\n\n## The covariate surface\n\nThe raster is read at the animals' `x` and `y` coordinates, so it must be in the same CRS as the region. We project the canopy cover layer and write it to a GeoTIFF. A file path is better than an in-memory raster for parallel runs, because each worker then reads the file itself instead of receiving a copy of the raster.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncanopy <- rast(here(\"data\", \"RMNP\", \"lidar\", \"lidar_pabove2_zmax_30m.tif\"))[[1]] |>\n  project(paste0(\"EPSG:\", LOCAL))\nnames(canopy) <- \"pabove2\"\ncanopy_file <- here(\"workflow\", \"outputs\", \"pabove2_local.tif\")\nwriteRaster(canopy, canopy_file, overwrite = TRUE)\n```\n:::\n\n\nAnimals outside the raster, here the 3.5% of the park without lidar data, get the raster mean, and `dsims` warns when this happens.\n\n## Detectability that depends on canopy\n\nWe use the fitted hazard-rate detection function for moose: the scale parameter at zero canopy cover (`scale_moose`), the shape parameter, and the canopy coefficient `beta_pabove2`. The name of the entry in `cov.surface` must match the name in `cov.param`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndetect_canopy <- make.detectability(\n  key.function = \"hr\",\n  scale.param = scale_moose,\n  shape.param = shape,\n  cov.param = list(pabove2 = beta_pabove2),\n  cov.surface = list(pabove2 = canopy_file),\n  truncation = TRUNCATION_M\n)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(detect_canopy, pop_desc)\n```\n\n::: {.cell-output-display}\n![Detection function for simulated moose at the mean canopy cover of the raster (solid), and at the canopy values that bound 95% of the raster (dashed).](simulation-detectability_files/figure-html/fig-detectability-1.png){#fig-detectability width=672}\n:::\n:::\n\n\nA single simulated survey shows the covariate at work. The canopy value at each detected animal is carried into the survey data, where the analysis can use it.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2025)\nsim_one <- make.simulation(reps = 1, design = design_full, population.description = pop_desc,\n                           detectability = detect_canopy,\n                           ds.analysis = make.ds.analysis(dfmodel = ~1, key = \"hr\", truncation = TRUNCATION_M))\nsurvey <- run.survey(sim_one)\ndetections <- survey@dist.data[!is.na(survey@dist.data$object), ]\nnrow(detections)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 229\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndetections$canopy_class <- cut(detections$pabove2, c(-Inf, 20, 30, Inf),\n                               labels = c(\"< 20%\", \"20-30%\", \"> 30%\"))\nggplot(detections, aes(distance)) +\n  geom_histogram(binwidth = 25, boundary = 0) +\n  facet_wrap(~canopy_class) +\n  labs(x = \"Distance (m)\", y = \"Detections\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![Distances of detections in one simulated survey, by canopy cover at the animal.](simulation-detectability_files/figure-html/fig-one-survey-1.png){#fig-one-survey width=672}\n:::\n:::\n\n\n## Analyses\n\nEach simulated survey is analysed in two ways. The first ignores canopy (`~1`) and relies on *pooling robustness*: the property that a detection function without covariates can still estimate density without bias when detectability varies between animals [@buckland2001]. The second offers AIC a choice between `~1` and `~pabove2`, as in the real analysis. Both use the hazard-rate key.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nanalysis_null <- make.ds.analysis(dfmodel = ~1, key = \"hr\", truncation = TRUNCATION_M)\nanalysis_canopy <- make.ds.analysis(dfmodel = list(~1, ~pabove2), key = c(\"hr\", \"hr\"),\n                                    truncation = TRUNCATION_M, criteria = \"AIC\")\n```\n:::\n\n\nFor the subplot designs, the subplots are strata of the design but should be analysed together, like the grouped strata in the `dsims` article [*Grouped strata*](https://distancesampling.org/dsims/articles/dsims_grouped_strata.html). `group.strata` maps every subplot to a single analysis stratum.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngroup_all <- data.frame(design.id = sub_region@strata.name, analysis.id = \"subplots\")\nanalysis_null_sub <- make.ds.analysis(dfmodel = ~1, key = \"hr\", truncation = TRUNCATION_M,\n                                      group.strata = group_all)\nanalysis_canopy_sub <- make.ds.analysis(dfmodel = list(~1, ~pabove2), key = c(\"hr\", \"hr\"),\n                                        truncation = TRUNCATION_M, criteria = \"AIC\",\n                                        group.strata = group_all)\n```\n:::\n\n\n## Running the simulations\n\nSix simulations combine the three designs with the two analyses, each with 100 repetitions. Every repetition generates a new population, a new set of transects and new detections, and fits the detection function. The simulations run in parallel and take several minutes each; for your own designs, start with 20 repetitions to check the setup.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nREPS <- 100\nset.seed(2025)\nrun <- function(design, pop, analysis) {\n  sim <- make.simulation(reps = REPS, design = design, population.description = pop,\n                         detectability = detect_canopy, ds.analysis = analysis)\n  run.simulation(sim, run.parallel = TRUE)\n}\n\nscenarios <- data.frame(\n  design = rep(c(\"whole park, systematic\", \"subplots, systematic\", \"subplots, zigzag\"), each = 2),\n  analysis = rep(c(\"~1\", \"AIC: ~1 or ~pabove2\"), 3)\n)\nsims <- list(\n  run(design_full, pop_desc, analysis_null),\n  run(design_full, pop_desc, analysis_canopy),\n  run(design_sys, pop_desc_sub, analysis_null_sub),\n  run(design_sys, pop_desc_sub, analysis_canopy_sub),\n  run(design_zz, pop_desc_sub, analysis_null_sub),\n  run(design_zz, pop_desc_sub, analysis_canopy_sub)\n)\nsaveRDS(list(scenarios = scenarios, sims = sims), here(\"workflow\", \"outputs\", \"simulations.rds\"))\n```\n:::\n\n\n## Results\n\nWe compare the designs on density, because the subplot designs estimate density within the subplots, not across the whole park. For each simulation we extract the true and mean estimated density (animals per km²), the bias, the root mean squared error relative to the true density (RRMSE), the proportion of 95% confidence intervals that contain the true value, and the mean coefficient of variation.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsim_metrics <- function(sim) {\n  s <- summary(sim, description.summary = FALSE)\n  D <- s@individuals$D\n  D <- D[nrow(D), ] # the total, or the single analysis stratum\n  data.frame(\n    true_D = D$Truth * 1e6,\n    mean_D = D$mean.Estimate * 1e6,\n    bias_pct = D$percent.bias,\n    rrmse = D$RMSE / D$Truth,\n    ci_coverage = D$CI.coverage.prob,\n    mean_cv = D$mean.se / D$mean.Estimate\n  )\n}\nresults <- cbind(scenarios, do.call(rbind, lapply(sims, sim_metrics)))\nknitr::kable(results, digits = 3)\n```\n\n::: {.cell-output-display}\n\n\n|design                 |analysis            | true_D| mean_D| bias_pct| rrmse| ci_coverage| mean_cv|\n|:----------------------|:-------------------|------:|------:|--------:|-----:|-----------:|-------:|\n|whole park, systematic |~1                  |  0.143|  0.146|    1.960| 0.099|        0.96|   0.103|\n|whole park, systematic |AIC: ~1 or ~pabove2 |  0.143|  0.146|    1.796| 0.106|        0.94|   0.094|\n|subplots, systematic   |~1                  |  0.145|  0.148|    2.372| 0.115|        0.96|   0.113|\n|subplots, systematic   |AIC: ~1 or ~pabove2 |  0.145|  0.147|    1.226| 0.100|        0.94|   0.106|\n|subplots, zigzag       |~1                  |  0.145|  0.145|   -0.215| 0.095|        0.93|   0.110|\n|subplots, zigzag       |AIC: ~1 or ~pabove2 |  0.145|  0.147|    1.623| 0.112|        0.92|   0.106|\n\n\n:::\n:::\n\n\nHow often did AIC choose the canopy model? The `Detection` results record which candidate model was selected in each repetition.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncanopy_share <- sapply(setNames(sims[c(2, 4, 6)], scenarios$design[c(2, 4, 6)]), function(sim) {\n  selected <- sim@results$Detection[1, \"SelectedModel\", seq_len(sim@reps)]\n  mean(selected == 2) # model 2 is ~pabove2\n})\ncanopy_share\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nwhole park, systematic   subplots, systematic       subplots, zigzag \n                  0.83                   0.72                   0.73 \n```\n\n\n:::\n:::\n\n\nWith 100 repetitions, the Monte Carlo standard error of the bias is about one percentage point: the standard deviation of the estimates divided by $\\sqrt{100}$. Differences in bias of a point or two are therefore not meaningful.\n\n- **Bias.** The bias ranges from -0.2% to 2.4%. That is small next to the CV of about 10% of a single survey, and no design or analysis stands out.\n- **Ignoring canopy.** Even when detectability depends on canopy, a detection function without covariates (`~1`) estimates density as well as one with it. This is pooling robustness at work: the detection function averages over the variation in detectability, and because each design samples canopy conditions in proportion to their extent in the area it estimates for, the average is the right one [@buckland2001]. Pooling robustness does not protect estimates for parts of the area with different canopy, such as density in dense forest alone [@rexstad2023].\n- **Including canopy.** AIC chose the canopy model in 72–83% of repetitions. Including it changes neither the bias nor the RRMSE materially, so for the overall density it does not make the estimate more accurate.\n- **Precision.** The RRMSE ranges from 0.09 to 0.11. The subplot designs estimate density within the subplots about as precisely as the whole-park design estimates it for the park, with less effort.\n- **Confidence intervals.** Coverage ranges from 0.92 to 0.96. With 100 repetitions, coverage itself has a Monte Carlo standard error of about 0.02, so this is consistent with the nominal 95% for all designs.\n\nThe estimates from each repetition are stored in `sim@results$individuals$D`, an array of strata × statistics × repetitions. Its last two slices hold the mean and standard deviation across repetitions, and the last row is the total.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nestimates <- do.call(rbind, lapply(seq_along(sims), function(i) {\n  est <- sims[[i]]@results$individuals$D\n  data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\", seq_len(sims[[i]]@reps)] * 1e6)\n}))\n```\n\n::: {.cell-output .cell-output-stderr}\n\n```\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\nWarning in data.frame(scenarios[i, ], D = est[dim(est)[1], \"Estimate\",\nseq_len(sims[[i]]@reps)] * : row names were found from a short variable and\nhave been discarded\n```\n\n\n:::\n\n```{.r .cell-code}\nggplot(estimates, aes(D, analysis)) +\n  geom_boxplot() +\n  geom_point(data = results, aes(true_D, analysis), shape = 124, size = 6, colour = \"red\") +\n  facet_wrap(~design, ncol = 1) +\n  labs(x = \"Estimated density (moose / km²)\", y = NULL) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![Density estimates across repetitions. The red mark is the true density of each simulation.](simulation-detectability_files/figure-html/fig-results-1.png){#fig-results width=672}\n:::\n:::\n\n\n## Discussion\n\nFor moose in Riding Mountain National Park, drone surveys confined to subplots sized for one flight estimate the density within the subplots as accurately as a design across the whole park. They do this with about 13% less transect, and the zigzag spends the largest share of flying time on effort ([*Survey designs with subplots*](simulation-design.qmd)). Canopy-dependent detectability, simulated here with the `cov.surface` argument, did not bias any design. Ignoring canopy in the analysis was also safe for the overall density.\n\nTwo caveats matter when using these results for planning:\n\n- **The estimate is for the subplots, not the park.** Here the subplots happen to have almost the same density as the park, but that is not guaranteed. To estimate abundance for the whole park from subplots, the survey data have to be modelled with a DSM, predicting from habitat covariates as in [*Fitting a density surface model*](dsm.qmd).\n- **The results are specific to this landscape.** The effect of spatially varying detectability depends on how canopy is distributed and where the transects fall. In denser forest, or with subplots placed in unrepresentative habitat, the conclusions could change. Checking that is exactly what `cov.surface` makes possible.\n\n## Saving\n\n\n::: {.cell}\n\n```{.r .cell-code}\nwrite.csv(results, here(\"workflow\", \"outputs\", \"simulation_results.csv\"), row.names = FALSE)\n```\n:::\n\n\n## References\n\n::: {#refs}\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}